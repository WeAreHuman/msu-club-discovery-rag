# Eval-only dependencies — NOT included in the main requirements.txt
# Install with: pip install -r eval/requirements-eval.txt

ragas>=0.2.0
datasets>=2.18.0
pandas>=2.0.0
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.0
